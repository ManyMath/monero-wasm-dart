name: monero_wasm
description: monero (monero-dart) but to Wasm for web.
version: 0.0.1-dev.1
repository: https://github.com/ManyMath/monero-wasm-dart

environment:
  sdk: ^3.6.0-216.1.beta

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
